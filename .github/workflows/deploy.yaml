name: deploy

on: push

jobs:
  first-job:
    runs-on: ubuntu-latest
    services:
      redis:
        image: redis
        ports:
          - 6379:6379
    steps:
      - name: install redis cli
        run: sido apt-get update -y && apt-get install redis-tools -y

      - name: print var
        run: echo ${{ secrets.TOKEN_SECRET }}
      - name: setup
        uses: actions/setup-node@v4
        with:
          node-version: 18
